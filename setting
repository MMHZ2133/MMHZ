<!DOCTYPE html>
<html lang="en" dir="ltr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>__USER_NAME__ - Connect Panel</title>
    <link href="https://fonts.googleapis.com/css2?family=Vazirmatn:wght@400;500;600;700;800;900&family=Inter:wght@400;500;600;700;800;900&display=swap" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/gh/itsyebekhe/nahan@main/tailwindcss.js"></script>
    <script>
        tailwind.config = {
            darkMode: 'class',
            theme: {
                extend: {
                    fontFamily: {
                        fa: ['Vazirmatn', 'sans-serif'],
                        en: ['Inter', 'sans-serif'],
                    },
                }
            }
        }
    </script>
    <style>
        /* ===== CSS Variables ===== */
        :root {
            /* Light Theme */
            --bg-body: #f0f2f5;
            --bg-card: rgba(255, 255, 255, 0.85);
            --bg-card-hover: rgba(255, 255, 255, 0.95);
            --bg-inner: #f8f9fa;
            --bg-input: #f1f3f5;
            --border-card: rgba(0, 0, 0, 0.06);
            --border-inner: rgba(0, 0, 0, 0.08);
            --border-focus: rgba(99, 102, 241, 0.4);
            --text-primary: #1a1a2e;
            --text-secondary: #4a4a6a;
            --text-muted: #8a8aaa;
            --accent: #6366f1;
            --accent-light: rgba(99, 102, 241, 0.12);
            --accent-hover: #4f46e5;
            --accent-gradient: linear-gradient(135deg, #6366f1, #8b5cf6);
            --shadow-card: 0 8px 32px rgba(0, 0, 0, 0.06);
            --shadow-hover: 0 12px 48px rgba(99, 102, 241, 0.12);
            --shadow-glow: 0 0 40px rgba(99, 102, 241, 0.15);
            --green: #10b981;
            --green-bg: rgba(16, 185, 129, 0.1);
            --green-border: rgba(16, 185, 129, 0.2);
            --amber: #f59e0b;
            --amber-bg: rgba(245, 158, 11, 0.1);
            --amber-border: rgba(245, 158, 11, 0.2);
            --red: #ef4444;
            --red-bg: rgba(239, 68, 68, 0.1);
            --red-border: rgba(239, 68, 68, 0.2);
            --progress-bg: #e5e7eb;
            --modal-overlay: rgba(0, 0, 0, 0.5);
            --modal-card: #ffffff;
            --radius: 20px;
            --radius-sm: 12px;
            --transition: 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        }

        /* Dark Theme */
        .dark {
            --bg-body: #0c0c1a;
            --bg-card: rgba(20, 20, 40, 0.85);
            --bg-card-hover: rgba(30, 30, 60, 0.95);
            --bg-inner: rgba(30, 30, 60, 0.5);
            --bg-input: rgba(10, 10, 26, 0.8);
            --border-card: rgba(99, 102, 241, 0.12);
            --border-inner: rgba(99, 102, 241, 0.08);
            --border-focus: rgba(99, 102, 241, 0.5);
            --text-primary: #eeefff;
            --text-secondary: #9999bb;
            --text-muted: #666688;
            --accent: #818cf8;
            --accent-light: rgba(99, 102, 241, 0.15);
            --accent-hover: #6366f1;
            --accent-gradient: linear-gradient(135deg, #818cf8, #a78bfa);
            --shadow-card: 0 8px 32px rgba(0, 0, 0, 0.4);
            --shadow-hover: 0 12px 48px rgba(99, 102, 241, 0.2);
            --shadow-glow: 0 0 60px rgba(99, 102, 241, 0.08);
            --green: #34d399;
            --green-bg: rgba(52, 211, 153, 0.1);
            --green-border: rgba(52, 211, 153, 0.2);
            --amber: #fbbf24;
            --amber-bg: rgba(251, 191, 36, 0.1);
            --amber-border: rgba(251, 191, 36, 0.2);
            --red: #f87171;
            --red-bg: rgba(248, 113, 113, 0.1);
            --red-border: rgba(248, 113, 113, 0.2);
            --progress-bg: rgba(30, 30, 60, 0.8);
            --modal-overlay: rgba(0, 0, 0, 0.8);
            --modal-card: #1a1a3a;
        }

        /* ===== Base ===== */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', 'Vazirmatn', sans-serif;
            background: var(--bg-body);
            color: var(--text-primary);
            transition: background var(--transition), color var(--transition);
            min-height: 100vh;
            padding: 24px 16px;
            display: flex;
            align-items: center;
            justify-content: center;
            -webkit-font-smoothing: antialiased;
        }

        [lang="fa"] body {
            font-family: 'Vazirmatn', sans-serif;
        }

        /* ===== Scrollbar ===== */
        ::-webkit-scrollbar { width: 4px; }
        ::-webkit-scrollbar-track { background: transparent; }
        ::-webkit-scrollbar-thumb { background: var(--accent); border-radius: 10px; }

        /* ===== Container ===== */
        .container {
            width: 100%;
            max-width: 880px;
            position: relative;
            z-index: 1;
        }

        /* ===== Cards ===== */
        .card {
            background: var(--bg-card);
            backdrop-filter: blur(20px);
            -webkit-backdrop-filter: blur(20px);
            border: 1px solid var(--border-card);
            border-radius: var(--radius);
            padding: 24px;
            transition: all var(--transition);
            box-shadow: var(--shadow-card);
        }

        .card:hover {
            box-shadow: var(--shadow-hover);
            background: var(--bg-card-hover);
        }

        .card-inner {
            background: var(--bg-inner);
            border: 1px solid var(--border-inner);
            border-radius: var(--radius-sm);
            padding: 16px 20px;
            transition: all var(--transition);
        }

        .card-glass {
            background: var(--bg-card);
            backdrop-filter: blur(20px);
            -webkit-backdrop-filter: blur(20px);
            border: 1px solid var(--border-card);
            border-radius: var(--radius);
            padding: 24px;
            box-shadow: var(--shadow-card);
            transition: all var(--transition);
        }

        /* ===== Glow Effects ===== */
        .glow {
            position: fixed;
            border-radius: 50%;
            filter: blur(100px);
            pointer-events: none;
            z-index: 0;
            opacity: 0.4;
        }

        .glow-1 {
            width: 400px;
            height: 400px;
            top: -100px;
            right: -100px;
            background: #818cf8;
            opacity: 0.12;
        }

        .glow-2 {
            width: 300px;
            height: 300px;
            bottom: -50px;
            left: -50px;
            background: #34d399;
            opacity: 0.08;
        }

        .dark .glow-1 { opacity: 0.08; }
        .dark .glow-2 { opacity: 0.06; }

        /* ===== Typography ===== */
        .title {
            font-size: 22px;
            font-weight: 900;
            letter-spacing: -0.5px;
            color: var(--text-primary);
        }

        .subtitle {
            font-size: 13px;
            color: var(--text-secondary);
            font-weight: 500;
        }

        .label {
            font-size: 10px;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 1.2px;
            color: var(--text-muted);
        }

        .value {
            font-size: 28px;
            font-weight: 900;
            letter-spacing: -0.5px;
            color: var(--text-primary);
        }

        .value-sm {
            font-size: 14px;
            font-weight: 700;
            color: var(--text-primary);
        }

        /* ===== Buttons ===== */
        .btn {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 6px;
            padding: 8px 18px;
            border-radius: var(--radius-sm);
            font-size: 11px;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            border: none;
            cursor: pointer;
            transition: all var(--transition);
            text-decoration: none;
            white-space: nowrap;
            background: var(--bg-inner);
            color: var(--text-secondary);
            border: 1px solid var(--border-inner);
        }

        .btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 16px rgba(0, 0, 0, 0.08);
        }

        .btn-primary {
            background: var(--accent-gradient);
            color: #fff;
            border: none;
            padding: 8px 20px;
        }

        .btn-primary:hover {
            box-shadow: 0 4px 24px rgba(99, 102, 241, 0.35);
            transform: translateY(-2px);
        }

        .btn-icon {
            width: 36px;
            height: 36px;
            padding: 0;
            display: flex;
            align-items: center;
            justify-content: center;
            border-radius: var(--radius-sm);
            background: transparent;
            border: 1px solid var(--border-inner);
            color: var(--text-muted);
            transition: all var(--transition);
            cursor: pointer;
        }

        .btn-icon:hover {
            border-color: var(--accent);
            color: var(--accent);
            background: var(--accent-light);
            transform: translateY(-2px);
        }

        /* ===== Input Group ===== */
        .input-group {
            display: flex;
            align-items: stretch;
            gap: 6px;
            background: var(--bg-input);
            border: 1px solid var(--border-inner);
            border-radius: var(--radius-sm);
            padding: 4px;
            transition: all var(--transition);
        }

        .input-group:focus-within {
            border-color: var(--accent);
            box-shadow: 0 0 0 3px var(--border-focus);
        }

        .input-group input {
            flex: 1;
            min-width: 0;
            padding: 10px 14px;
            background: transparent;
            border: none;
            color: var(--text-primary);
            font-size: 12px;
            font-family: 'Inter', monospace;
            outline: none;
            letter-spacing: 0.3px;
        }

        .input-group input::selection {
            background: var(--accent);
            color: #fff;
        }

        /* ===== Tags ===== */
        .tag {
            font-size: 8px;
            font-weight: 800;
            text-transform: uppercase;
            letter-spacing: 1.2px;
            padding: 3px 12px;
            border-radius: 20px;
            border: 1px solid var(--border-inner);
            background: var(--bg-inner);
            color: var(--text-muted);
            white-space: nowrap;
        }

        .tag-primary {
            background: var(--accent-light);
            color: var(--accent);
            border-color: var(--border-focus);
        }

        .tag-green {
            background: var(--green-bg);
            color: var(--green);
            border-color: var(--green-border);
        }

        .tag-amber {
            background: var(--amber-bg);
            color: var(--amber);
            border-color: var(--amber-border);
        }

        .tag-red {
            background: var(--red-bg);
            color: var(--red);
            border-color: var(--red-border);
        }

        /* ===== Status Badge ===== */
        .badge {
            display: inline-block;
            padding: 5px 16px;
            border-radius: 20px;
            font-size: 10px;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 0.8px;
            border: 1px solid var(--border-inner);
            background: var(--bg-inner);
            color: var(--text-secondary);
        }

        .badge-active {
            background: var(--green-bg);
            color: var(--green);
            border-color: var(--green-border);
        }

        .badge-paused {
            background: var(--amber-bg);
            color: var(--amber);
            border-color: var(--amber-border);
        }

        .badge-expired {
            background: var(--red-bg);
            color: var(--red);
            border-color: var(--red-border);
        }

        /* ===== Progress ===== */
        .progress {
            height: 4px;
            border-radius: 4px;
            background: var(--progress-bg);
            overflow: hidden;
            margin-top: 8px;
        }

        .progress-fill {
            height: 100%;
            border-radius: 4px;
            background: var(--accent-gradient);
            transition: width 1s ease;
        }

        /* ===== Avatar ===== */
        .avatar {
            width: 64px;
            height: 64px;
            border-radius: 16px;
            display: flex;
            align-items: center;
            justify-content: center;
            background: var(--accent-light);
            border: 2px solid var(--border-focus);
            color: var(--accent);
            flex-shrink: 0;
        }

        .avatar svg {
            width: 32px;
            height: 32px;
        }

        /* ===== Grid ===== */
        .grid-2 {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 16px;
        }

        .grid-4 {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 10px;
        }

        /* ===== Modal ===== */
        .modal-overlay {
            position: fixed;
            inset: 0;
            background: var(--modal-overlay);
            backdrop-filter: blur(12px);
            -webkit-backdrop-filter: blur(12px);
            display: none;
            align-items: center;
            justify-content: center;
            padding: 24px;
            z-index: 100;
            opacity: 0;
            transition: opacity 0.3s ease;
        }

        .modal-overlay.show {
            display: flex;
            opacity: 1;
        }

        .modal-card {
            background: var(--modal-card);
            border: 1px solid var(--border-card);
            border-radius: var(--radius);
            padding: 32px;
            max-width: 400px;
            width: 100%;
            transform: scale(0.95);
            transition: transform 0.3s ease;
            box-shadow: 0 24px 80px rgba(0, 0, 0, 0.3);
            position: relative;
        }

        .modal-overlay.show .modal-card {
            transform: scale(1);
        }

        .modal-close {
            position: absolute;
            top: 16px;
            right: 16px;
            background: none;
            border: none;
            color: var(--text-muted);
            cursor: pointer;
            padding: 4px;
            border-radius: 8px;
            transition: all var(--transition);
        }

        .modal-close:hover {
            background: var(--bg-inner);
            color: var(--text-primary);
        }

        /* ===== Toast ===== */
        .toast {
            position: fixed;
            top: 24px;
            left: 50%;
            transform: translateX(-50%) translateY(-16px);
            background: var(--bg-card);
            backdrop-filter: blur(20px);
            border: 1px solid var(--border-card);
            border-radius: var(--radius-sm);
            padding: 12px 24px;
            box-shadow: var(--shadow-hover);
            font-size: 13px;
            font-weight: 600;
            color: var(--text-primary);
            display: flex;
            align-items: center;
            gap: 10px;
            z-index: 200;
            opacity: 0;
            pointer-events: none;
            transition: all 0.4s ease;
        }

        .toast.show {
            opacity: 1;
            transform: translateX(-50%) translateY(0);
            pointer-events: auto;
        }

        .toast-icon {
            color: var(--green);
            flex-shrink: 0;
        }

        /* ===== Responsive ===== */
        @media (max-width: 768px) {
            body { padding: 16px 12px; }
            .card { padding: 18px; }
            .grid-2 { grid-template-columns: 1fr; }
            .grid-4 { grid-template-columns: repeat(2, 1fr); }
            .value { font-size: 24px; }
            .title { font-size: 19px; }
            .avatar { width: 52px; height: 52px; }
            .avatar svg { width: 26px; height: 26px; }
            .modal-card { padding: 24px; margin: 16px; }
        }

        @media (max-width: 480px) {
            .grid-4 { grid-template-columns: 1fr 1fr; gap: 8px; }
            .input-group input { font-size: 10px; padding: 8px 10px; }
            .btn { font-size: 9px; padding: 6px 12px; }
        }

        /* ===== Utility ===== */
        .flex { display: flex; }
        .flex-center { display: flex; align-items: center; justify-content: center; }
        .flex-between { display: flex; align-items: center; justify-content: space-between; }
        .gap-1 { gap: 6px; }
        .gap-2 { gap: 12px; }
        .gap-3 { gap: 16px; }
        .gap-4 { gap: 24px; }
        .mt-1 { margin-top: 6px; }
        .mt-2 { margin-top: 12px; }
        .mt-3 { margin-top: 16px; }
        .mt-4 { margin-top: 24px; }
        .mb-1 { margin-bottom: 6px; }
        .mb-2 { margin-bottom: 12px; }
        .mb-3 { margin-bottom: 16px; }
        .mb-4 { margin-bottom: 24px; }
        .text-center { text-align: center; }
        .text-right { text-align: right; }
        .w-full { width: 100%; }
        .relative { position: relative; }
        .hidden { display: none; }

        .divider {
            height: 1px;
            background: var(--border-inner);
            margin: 16px 0;
        }

        .chip {
            display: inline-flex;
            align-items: center;
            gap: 4px;
            padding: 2px 10px;
            border-radius: 12px;
            font-size: 9px;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            background: var(--bg-inner);
            color: var(--text-muted);
            border: 1px solid var(--border-inner);
        }

        /* ===== Animations ===== */
        @keyframes fadeInUp {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .animate-in {
            animation: fadeInUp 0.6s ease forwards;
        }

        .delay-1 { animation-delay: 0.05s; opacity: 0; }
        .delay-2 { animation-delay: 0.1s; opacity: 0; }
        .delay-3 { animation-delay: 0.15s; opacity: 0; }
        .delay-4 { animation-delay: 0.2s; opacity: 0; }
    </style>
</head>
<body>

    <!-- ===== Glow Effects ===== -->
    <div class="glow glow-1"></div>
    <div class="glow glow-2"></div>

    <!-- ===== Toast ===== -->
    <div id="toast" class="toast">
        <svg class="toast-icon" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
            <path d="M20 6L9 17l-5-5"/>
        </svg>
        <span id="toast-msg">Copied!</span>
    </div>

    <!-- ===== Modal ===== -->
    <div id="qr-modal" class="modal-overlay">
        <div class="modal-card">
            <button class="modal-close" onclick="closeQR()">
                <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                    <line x1="18" y1="6" x2="6" y2="18"/>
                    <line x1="6" y1="6" x2="18" y2="18"/>
                </svg>
            </button>
            <h3 class="title text-center mb-3" id="qr-title">Scan QR</h3>
            <div class="flex-center">
                <img id="qr-img" src="" alt="QR Code" class="w-48 h-48 sm:w-52 sm:h-52" style="border-radius: 12px; background: white; padding: 8px;">
            </div>
            <p id="qr-text" class="text-center text-xs text-muted mt-3 break-all" style="font-family: monospace;"></p>
            <button onclick="closeQR()" class="btn btn-primary w-full mt-4">Close</button>
        </div>
    </div>

    <!-- ===== Main Container ===== -->
    <div class="container">

        <!-- ===== Header ===== -->
        <div class="card animate-in delay-1">
            <div class="flex-between">
                <div class="flex gap-2">
                    <div style="width: 40px; height: 40px; border-radius: 12px; background: var(--accent-gradient); display: flex; align-items: center; justify-content: center; flex-shrink: 0;">
                        <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
                            <path d="M12 2L2 7l10 5 10-5-10-5z"/>
                            <path d="M2 17l10 5 10-5"/>
                            <path d="M2 12l10 5 10-5"/>
                        </svg>
                    </div>
                    <div>
                        <div class="title">Connect Panel</div>
                        <div class="subtitle">Manage your subscription</div>
                    </div>
                </div>
                <div class="flex gap-1">
                    <button onclick="toggleTheme()" class="btn" id="theme-btn">
                        <span id="theme-icon">🌙</span>
                        <span id="theme-label">Dark</span>
                    </button>
                    <button onclick="toggleLang()" class="btn" id="lang-btn">
                        <span id="lang-icon">🇺🇸</span>
                        <span id="lang-label">EN</span>
                    </button>
                </div>
            </div>
        </div>

        <!-- ===== Profile ===== -->
        <div class="card animate-in delay-2 mt-2">
            <div class="flex" style="gap: 18px; flex-wrap: wrap;">
                <div class="avatar">
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                        <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/>
                        <circle cx="12" cy="7" r="4"/>
                    </svg>
                </div>
                <div style="flex: 1; min-width: 140px;">
                    <div class="title" style="font-size: 20px;">__USER_NAME__</div>
                    <div class="subtitle" style="font-size: 12px; font-family: monospace;">ID: __USER_ID__</div>
                    <div class="flex gap-2 mt-1" style="flex-wrap: wrap;">
                        <span id="status-badge" class="badge badge-active">Active</span>
                        <span class="subtitle" style="font-size: 11px;">Expires: <strong style="color: var(--text-primary);">__EXPIRY_DATE__</strong></span>
                    </div>
                </div>
                <div style="text-align: right; flex-shrink: 0;">
                    <div class="label">Connection IP</div>
                    <div class="value-sm" style="font-family: monospace;" id="client-ip">—</div>
                </div>
            </div>
        </div>

        <!-- ===== Stats ===== -->
        <div class="grid-2 animate-in delay-3 mt-2">
            <div class="card">
                <div class="flex-between">
                    <span class="label" data-i18n="total">Total Usage</span>
                    <span class="subtitle" style="font-size: 11px;">/ __LIMIT_TOTAL_GB__ GB</span>
                </div>
                <div class="value mt-1">__TOTAL_GB__</div>
                __TOTAL_PROGRESS__
            </div>
            <div class="card">
                <div class="flex-between">
                    <span class="label" data-i18n="daily">Daily Usage</span>
                    <span class="subtitle" style="font-size: 11px;">/ __LIMIT_DAILY_GB__ GB</span>
                </div>
                <div class="value mt-1">__DAILY_GB__</div>
                __DAILY_PROGRESS__
            </div>
        </div>

        <!-- ===== Subscription ===== -->
        <div class="card animate-in delay-4 mt-2">
            <div class="flex-between mb-2">
                <div>
                    <div class="label" data-i18n="sub">Subscription URL</div>
                    <div class="subtitle" data-i18n="sub-desc">Import this link into your client</div>
                </div>
                <span class="tag tag-primary">v2</span>
            </div>

            <!-- Main -->
            <div class="input-group">
                <input type="text" id="link-main" value="__SYNC_NORMAL__" readonly>
                <button onclick="copyLink('link-main')" class="btn btn-primary">Copy</button>
                <button onclick="showQR('__SYNC_NORMAL__')" class="btn-icon">
                    <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                        <rect x="3" y="3" width="7" height="7"/>
                        <rect x="14" y="3" width="7" height="7"/>
                        <rect x="3" y="14" width="7" height="7"/>
                        <line x1="14" y1="14" x2="14" y2="21"/>
                        <line x1="21" y1="14" x2="21" y2="21"/>
                        <line x1="14" y1="17" x2="21" y2="17"/>
                    </svg>
                </button>
            </div>

            <!-- Formats -->
            <div class="divider"></div>

            <div class="label mb-2" data-i18n="formats">Formats</div>

            <div style="display: flex; flex-direction: column; gap: 8px;">
                <!-- Base64 -->
                <div class="flex" style="gap: 8px; align-items: center;">
                    <span class="tag tag-green">Base64</span>
                    <div class="input-group" style="flex: 1;">
                        <input type="text" id="link-base64" value="__SYNC_NORMAL__" readonly>
                        <button onclick="copyLink('link-base64')" class="btn">Copy</button>
                        <button onclick="showQR(document.getElementById('link-base64').value)" class="btn-icon">
                            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                                <rect x="3" y="3" width="7" height="7"/>
                                <rect x="14" y="3" width="7" height="7"/>
                                <rect x="3" y="14" width="7" height="7"/>
                                <line x1="14" y1="14" x2="14" y2="21"/>
                                <line x1="21" y1="14" x2="21" y2="21"/>
                                <line x1="14" y1="17" x2="21" y2="17"/>
                            </svg>
                        </button>
                    </div>
                </div>

                <!-- Clash -->
                <div class="flex" style="gap: 8px; align-items: center;">
                    <span class="tag" style="background: rgba(139, 92, 246, 0.12); color: #8b5cf6; border-color: rgba(139, 92, 246, 0.2);">Clash</span>
                    <div class="input-group" style="flex: 1;">
                        <input type="text" id="link-clash" value="__SYNC_NORMAL__" readonly>
                        <button onclick="copyLink('link-clash')" class="btn">Copy</button>
                        <button onclick="showQR(document.getElementById('link-clash').value)" class="btn-icon">
                            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                                <rect x="3" y="3" width="7" height="7"/>
                                <rect x="14" y="3" width="7" height="7"/>
                                <rect x="3" y="14" width="7" height="7"/>
                                <line x1="14" y1="14" x2="14" y2="21"/>
                                <line x1="21" y1="14" x2="21" y2="21"/>
                                <line x1="14" y1="17" x2="21" y2="17"/>
                            </svg>
                        </button>
                    </div>
                </div>

                <!-- Sing-box -->
                <div class="flex" style="gap: 8px; align-items: center;">
                    <span class="tag tag-amber">Sing-box</span>
                    <div class="input-group" style="flex: 1;">
                        <input type="text" id="link-singbox" value="__SYNC_NORMAL__" readonly>
                        <button onclick="copyLink('link-singbox')" class="btn">Copy</button>
                        <button onclick="showQR(document.getElementById('link-singbox').value)" class="btn-icon">
                            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                                <rect x="3" y="3" width="7" height="7"/>
                                <rect x="14" y="3" width="7" height="7"/>
                                <rect x="3" y="14" width="7" height="7"/>
                                <line x1="14" y1="14" x2="14" y2="21"/>
                                <line x1="21" y1="14" x2="21" y2="21"/>
                                <line x1="14" y1="17" x2="21" y2="17"/>
                            </svg>
                        </button>
                    </div>
                </div>

                <!-- Raw -->
                <div class="flex" style="gap: 8px; align-items: center;">
                    <span class="tag tag-red">Raw</span>
                    <div class="input-group" style="flex: 1;">
                        <input type="text" id="link-raw" value="__SYNC_NORMAL__" readonly>
                        <button onclick="copyLink('link-raw')" class="btn">Copy</button>
                        <button onclick="showQR(document.getElementById('link-raw').value)" class="btn-icon">
                            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                                <rect x="3" y="3" width="7" height="7"/>
                                <rect x="14" y="3" width="7" height="7"/>
                                <rect x="3" y="14" width="7" height="7"/>
                                <line x1="14" y1="14" x2="14" y2="21"/>
                                <line x1="21" y1="14" x2="21" y2="21"/>
                                <line x1="14" y1="17" x2="21" y2="17"/>
                            </svg>
                        </button>
                    </div>
                </div>
            </div>
        </div>

        <!-- ===== One-Click ===== -->
        <div class="card mt-2 animate-in delay-4">
            <div class="label mb-2" data-i18n="oneclick">One-Click Import</div>
            <div class="subtitle mb-2" data-i18n="oneclick-desc">Quick import for supported clients</div>
            <div class="grid-4">
                <a id="v2rayng-link" href="#" class="btn" style="justify-content: center; gap: 8px;">
                    <span>v2rayNG</span>
                    <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                        <path d="M7 7h10v10"/>
                        <path d="M7 17L17 7"/>
                    </svg>
                </a>
                <a href="#" onclick="window.location.href='v2rayn://install-sub?url='+encodeURIComponent('__SYNC_NORMAL__')+'&name=__USER_NAME__'" class="btn" style="justify-content: center; gap: 8px;">
                    <span>v2rayN</span>
                    <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                        <path d="M7 7h10v10"/>
                        <path d="M7 17L17 7"/>
                    </svg>
                </a>
                <a href="#" onclick="window.location.href='shadowrocket://add/sub://'+btoa('__SYNC_NORMAL__')+'?title=__USER_NAME__'" class="btn" style="justify-content: center; gap: 8px;">
                    <span>Shadowrocket</span>
                    <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                        <path d="M7 7h10v10"/>
                        <path d="M7 17L17 7"/>
                    </svg>
                </a>
                <a href="#" onclick="window.location.href='streisand://import/__SYNC_NORMAL__'" class="btn" style="justify-content: center; gap: 8px;">
                    <span>Streisand</span>
                    <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                        <path d="M7 7h10v10"/>
                        <path d="M7 17L17 7"/>
                    </svg>
                </a>
            </div>
        </div>

        <!-- ===== Footer ===== -->
        <div class="text-center mt-4">
            <span class="subtitle" style="font-size: 11px;">Connect Panel &bull; <span id="footer-year">2024</span></span>
        </div>

    </div>

    <script>
        // ============================================================
        // I18N
        // ============================================================
        const I18N = {
            en: {
                total: 'Total Usage',
                daily: 'Daily Usage',
                sub: 'Subscription URL',
                'sub-desc': 'Import this link into your client',
                formats: 'Formats',
                oneclick: 'One-Click Import',
                'oneclick-desc': 'Quick import for supported clients',
                copied: 'Copied!',
                qr: 'Scan QR',
                close: 'Close',
                active: 'Active',
                paused: 'Paused',
                expired: 'Expired',
                limit: 'Limit Exceeded',
                dailylimit: 'Daily Limit Exceeded'
            },
            fa: {
                total: 'مصرف کل',
                daily: 'مصرف روزانه',
                sub: 'لینک اشتراک',
                'sub-desc': 'این لینک را در کلاینت خود وارد کنید',
                formats: 'فرمت‌ها',
                oneclick: 'وارد کردن سریع',
                'oneclick-desc': 'وارد کردن سریع برای کلاینت‌های پشتیبانی شده',
                copied: 'کپی شد!',
                qr: 'اسکن QR',
                close: 'بستن',
                active: 'فعال',
                paused: 'متوقف',
                expired: 'منقضی',
                limit: 'از حد مجاز رد شده',
                dailylimit: 'از حد روزانه رد شده'
            }
        };

        // ============================================================
        // State
        // ============================================================
        let lang = 'en';
        let dark = true;

        // ============================================================
        // DOM Ref
        // ============================================================
        const $ = id => document.getElementById(id);
        const qs = (sel, ctx) => (ctx || document).querySelector(sel);
        const qsa = (sel, ctx) => (ctx || document).querySelectorAll(sel);

        // ============================================================
        // Theme
        // ============================================================
        function applyTheme() {
            const root = document.documentElement;
            const icon = $('theme-icon');
            const label = $('theme-label');

            if (dark) {
                root.classList.add('dark');
                icon.textContent = '☀️';
                label.textContent = lang === 'fa' ? 'روشن' : 'Light';
            } else {
                root.classList.remove('dark');
                icon.textContent = '🌙';
                label.textContent = lang === 'fa' ? 'تاریک' : 'Dark';
            }
            try { localStorage.setItem('sp-theme', dark ? 'dark' : 'light'); } catch (_) {}
        }

        function toggleTheme() {
            dark = !dark;
            applyTheme();
        }

        // ============================================================
        // Language
        // ============================================================
        function applyLang() {
            const t = I18N[lang];
            qsa('[data-i18n]').forEach(el => {
                const key = el.getAttribute('data-i18n');
                if (t[key] !== undefined) el.textContent = t[key];
            });

            const icon = $('lang-icon');
            const label = $('lang-label');
            const html = document.documentElement;

            if (lang === 'fa') {
                html.setAttribute('dir', 'rtl');
                html.setAttribute('lang', 'fa');
                icon.textContent = '🇮🇷';
                label.textContent = 'FA';
            } else {
                html.setAttribute('dir', 'ltr');
                html.setAttribute('lang', 'en');
                icon.textContent = '🇺🇸';
                label.textContent = 'EN';
            }

            updateBadge();
            try { localStorage.setItem('sp-lang', lang); } catch (_) {}
        }

        function toggleLang() {
            lang = lang === 'en' ? 'fa' : 'en';
            applyLang();
            applyTheme();
        }

        // ============================================================
        // Badge
        // ============================================================
        function updateBadge() {
            const badge = $('status-badge');
            if (!badge) return;
            const t = I18N[lang];
            const code = '__STATUS_CODE__';
            const map = {
                active: { text: t.active || 'Active', cls: 'badge-active' },
                paused: { text: t.paused || 'Paused', cls: 'badge-paused' },
                expired: { text: t.expired || 'Expired', cls: 'badge-expired' },
                limit: { text: t.limit || 'Limit Exceeded', cls: 'badge-expired' },
                dailylimit: { text: t.dailylimit || 'Daily Limit Exceeded', cls: 'badge-expired' }
            };
            const s = map[code] || map.active;
            badge.textContent = s.text;
            badge.className = 'badge ' + s.cls;
        }

        // ============================================================
        // Toast
        // ============================================================
        let toastTimer = null;

        function showToast(msg) {
            const el = $('toast');
            const text = $('toast-msg');
            if (!el || !text) return;
            text.textContent = msg || I18N[lang].copied || 'Copied!';
            el.classList.add('show');
            clearTimeout(toastTimer);
            toastTimer = setTimeout(() => el.classList.remove('show'), 2200);
        }

        // ============================================================
        // Copy
        // ============================================================
        function copyLink(id) {
            const el = $(id);
            if (!el) return;
            el.select();
            el.setSelectionRange(0, 99999);
            try {
                navigator.clipboard.writeText(el.value).then(() => showToast());
            } catch (_) {
                document.execCommand('copy');
                showToast();
            }
        }

        // ============================================================
        // QR
        // ============================================================
        function showQR(url) {
            const modal = $('qr-modal');
            const img = $('qr-img');
            const text = $('qr-text');
            const title = $('qr-title');
            if (!modal || !img || !text) return;

            title.textContent = I18N[lang].qr || 'Scan QR';
            text.textContent = url;
            img.src = 'https://api.qrserver.com/v1/create-qr-code/?size=300x300&data=' + encodeURIComponent(url);

            modal.classList.add('show');
            document.body.style.overflow = 'hidden';
        }

        function closeQR() {
            const modal = $('qr-modal');
            if (!modal) return;
            modal.classList.remove('show');
            document.body.style.overflow = '';
        }

        // Close modal on backdrop click
        document.addEventListener('click', (e) => {
            const modal = $('qr-modal');
            if (e.target === modal) closeQR();
        });

        // ============================================================
        // Init
        // ============================================================
        (function init() {
            // Load saved preferences
            try {
                const savedTheme = localStorage.getItem('sp-theme');
                if (savedTheme) dark = savedTheme === 'dark';
            } catch (_) {}
            try {
                const savedLang = localStorage.getItem('sp-lang');
                if (savedLang && I18N[savedLang]) lang = savedLang;
            } catch (_) {}

            // Apply
            applyTheme();
            applyLang();

            // Set year
            const year = $('footer-year');
            if (year) year.textContent = new Date().getFullYear();

            // Populate format links with flags
            const main = $('link-main');
            if (main) {
                const base = main.value;
                const hasQuery = base.includes('?');
                const q = hasQuery ? '&' : '?';

                const setVal = (id, flag) => {
                    const el = $(id);
                    if (el) el.value = base + q + 'flag=' + flag;
                };

                setVal('link-base64', 'base64');
                setVal('link-clash', 'clash');
                setVal('link-singbox', 'singbox');
                setVal('link-raw', 'raw');

                // Update v2rayNG link
                const v2 = $('v2rayng-link');
                if (v2) {
                    const base64Url = base + q + 'flag=base64';
                    v2.href = 'v2rayng://install-sub?url=' + encodeURIComponent(base64Url) + '&name=' + encodeURIComponent('__USER_NAME__');
                }
            }

            // Fetch IP
            fetch('https://api.ipify.org?format=json')
                .then(r => r.json())
                .then(data => {
                    const ip = $('client-ip');
                    if (ip) ip.textContent = data.ip || '—';
                })
                .catch(() => {
                    const ip = $('client-ip');
                    if (ip) ip.textContent = '—';
                });
        })();
    </script>

</body>
</html>
