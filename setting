<!DOCTYPE html>
<html lang="fa" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>VaslPlus | __USER_NAME__</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link href="https://fonts.googleapis.com/css2?family=Vazirmatn:wght@400;600;700;900&display=swap" rel="stylesheet">
    <style>
        /* ==================== Variables ==================== */
        :root {
            --accent: #ff2338;
            --accent-bright: #ff4d5e;
            --accent-glow: rgba(255, 35, 56, 0.4);
            --accent-soft: rgba(255, 35, 56, 0.12);
            --accent-border: rgba(255, 35, 56, 0.35);
            --text-primary: #ffffff;
            --text-muted: #6b6b80;
            --text-secondary: #a0a0b0;
            --border: #23232f;
            --ring-bg: #23232f;
            --gradient-silver: linear-gradient(135deg, #ffffff 0%, #b8b8c8 100%);
            --rgb-gradient: linear-gradient(90deg, #ff0000, #ff8000, #ffff00, #00ff00, #00ffff, #0080ff, #8000ff, #ff00ff, #ff0000);

            /* Velvet backgrounds - Dark (مشکی-قرمز مخملی) */
            --velvet-bg: radial-gradient(ellipse at top left, #2a0a12 0%, #1a060d 30%, #0a0a0f 60%, #050507 100%);
            --velvet-overlay: radial-gradient(circle at 20% 30%, rgba(255, 35, 56, 0.08) 0%, transparent 50%),
                              radial-gradient(circle at 80% 70%, rgba(255, 107, 53, 0.06) 0%, transparent 50%);
        }

        html.light {
            --accent: #0d6efd;
            --accent-bright: #3b82f6;
            --accent-glow: rgba(13, 110, 253, 0.35);
            --accent-soft: rgba(13, 110, 253, 0.12);
            --accent-border: rgba(13, 110, 253, 0.35);
            --text-primary: #0a2540;
            --text-muted: #94a3b8;
            --text-secondary: #475569;
            --border: #dbeafe;
            --ring-bg: #dbeafe;
            --gradient-silver: linear-gradient(135deg, #0a2540 0%, #1e40af 100%);

            /* Velvet backgrounds - Light (آبی-سفید مخملی) */
            --velvet-bg: radial-gradient(ellipse at top left, #dbeafe 0%, #eff6ff 30%, #f0f9ff 60%, #ffffff 100%);
            --velvet-overlay: radial-gradient(circle at 20% 30%, rgba(13, 110, 253, 0.08) 0%, transparent 50%),
                              radial-gradient(circle at 80% 70%, rgba(59, 130, 246, 0.06) 0%, transparent 50%);
        }

        * { margin: 0; padding: 0; box-sizing: border-box; }

        html, body {
            font-family: 'Vazirmatn', sans-serif;
            color: var(--text-primary);
            min-height: 100vh;
            overflow-x: hidden;
            transition: color 0.4s;
        }

        /* ==================== Velvet Background ==================== */
        body::before {
            content: '';
            position: fixed;
            inset: 0;
            z-index: -2;
            background: var(--velvet-bg);
            transition: background 0.5s ease;
        }
        body::after {
            content: '';
            position: fixed;
            inset: 0;
            z-index: -1;
            background: var(--velvet-overlay);
            transition: background 0.5s ease;
            pointer-events: none;
        }

        /* ==================== Top Controls ==================== */
        .top-controls {
            position: fixed;
            top: 20px;
            left: 20px;
            display: flex;
            gap: 8px;
            z-index: 100;
        }
        .ctrl-btn {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 6px;
            padding: 9px 12px;
            background: rgba(20, 20, 28, 0.75);
            backdrop-filter: blur(10px);
            -webkit-backdrop-filter: blur(10px);
            border: 1px solid var(--border);
            border-radius: 10px;
            color: var(--text-secondary);
            font-family: inherit;
            font-size: 12px;
            font-weight: 700;
            cursor: pointer;
            transition: border-color 0.2s, color 0.2s;
        }
        html.light .ctrl-btn {
            background: rgba(255, 255, 255, 0.8);
        }
        .ctrl-btn:hover {
            border-color: var(--accent);
            color: var(--accent-bright);
        }
        .ctrl-btn svg { width: 18px; height: 18px; }

        .ctrl-btn.support-btn {
            background: linear-gradient(135deg, #0088cc, #00a8e8);
            border-color: #0088cc;
            color: #ffffff;
        }
        .ctrl-btn.support-btn:hover {
            border-color: #00a8e8;
            color: #ffffff;
        }

        /* ==================== Container ==================== */
        .container {
            position: relative;
            z-index: 1;
            width: 100%;
            max-width: 640px;
            margin: 0 auto;
            padding: 100px 16px 24px;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
        }

        /* ==================== Logo with RGB Ring ==================== */
        .logo-header { text-align: center; margin-bottom: 36px; }

        .logo-ring {
            position: relative;
            width: 92px;
            height: 92px;
            margin: 0 auto 16px;
            border-radius: 50%;
            padding: 3px;
            background: conic-gradient(from 0deg,
                #ff0000, #ff8000, #ffff00, #00ff00,
                #00ffff, #0080ff, #8000ff, #ff00ff, #ff0000);
            animation: rgbRotate 4s linear infinite;
            box-shadow: 0 0 20px rgba(255, 0, 128, 0.3);
        }
        @keyframes rgbRotate {
            from { filter: hue-rotate(0deg); }
            to { filter: hue-rotate(360deg); }
        }
        .logo-mark {
            width: 100%;
            height: 100%;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            background: var(--velvet-bg);
        }
        html.light .logo-mark {
            background: #ffffff;
        }
        .logo-mark svg { width: 44px; height: 44px; }
        .logo-mark .logo-swoosh { stroke: var(--accent-bright); }

        .brand-name {
            font-size: 26px;
            font-weight: 900;
            letter-spacing: 0.5px;
            line-height: 1;
            direction: ltr;
        }
        .brand-name .vasl {
            background: var(--gradient-silver);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }
        .brand-name .plus {
            color: var(--accent-bright);
            text-shadow: 0 0 20px var(--accent-glow);
        }
        .brand-tagline {
            font-size: 10px;
            font-weight: 600;
            letter-spacing: 3px;
            color: var(--text-muted);
            margin-top: 6px;
            direction: ltr;
        }
        .brand-tagline span { color: var(--accent); margin: 0 6px; }

        /* ==================== Greeting ==================== */
        .user-greeting {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
            margin-bottom: 24px;
            font-size: 14px;
            color: var(--text-secondary);
        }
        .user-greeting strong { color: var(--text-primary); font-weight: 700; }

        /* ==================== Cards with RGB Border ==================== */
        .cards-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 16px;
        }

        .card-rgb {
            position: relative;
            border-radius: 16px;
            padding: 2px;
            background: conic-gradient(from 0deg,
                #ff0000, #ff8000, #ffff00, #00ff00,
                #00ffff, #0080ff, #8000ff, #ff00ff, #ff0000);
            animation: rgbRotate 4s linear infinite;
            box-shadow: 0 0 20px rgba(255, 0, 128, 0.15);
        }

        .card {
            background: rgba(20, 20, 28, 0.75);
            backdrop-filter: blur(12px);
            -webkit-backdrop-filter: blur(12px);
            border-radius: 14px;
            padding: 22px 20px;
            position: relative;
            overflow: hidden;
            height: 100%;
        }
        html.light .card {
            background: rgba(255, 255, 255, 0.85);
        }

        .card-icon {
            width: 42px;
            height: 42px;
            border-radius: 10px;
            display: flex;
            align-items: center;
            justify-content: center;
            background: var(--accent-soft);
            border: 1px solid var(--accent-border);
            color: var(--accent-bright);
            margin-bottom: 14px;
        }
        .card-icon svg { width: 20px; height: 20px; }

        .card-label {
            font-size: 12px;
            font-weight: 600;
            color: var(--text-secondary);
            margin-bottom: 10px;
        }

        .card-value {
            font-size: 28px;
            font-weight: 900;
            line-height: 1.1;
            display: flex;
            align-items: baseline;
            gap: 5px;
            direction: ltr;
            flex-wrap: wrap;
        }
        .card-value .num { color: var(--text-primary); }
        .card-value .unit { font-size: 13px; font-weight: 600; color: var(--text-muted); }
        .card-value .unit-accent { color: var(--accent-bright); }
        .card-value .infinity {
            font-size: 42px;
            font-weight: 900;
            color: var(--accent-bright);
            text-shadow: 0 0 20px var(--accent-glow);
            line-height: 0.8;
        }
        .card-value .infinity-text {
            font-size: 18px;
            font-weight: 700;
            color: var(--accent-bright);
            margin-right: 6px;
        }

        /* ==================== RGB Progress Bars ==================== */
        .rgb-progress {
            position: relative;
            height: 8px;
            border-radius: 8px;
            background: var(--ring-bg);
            overflow: hidden;
            margin-top: 16px;
        }
        .rgb-progress-fill {
            height: 100%;
            border-radius: 8px;
            background: var(--rgb-gradient);
            background-size: 300% 100%;
            animation: rgbSlide 3s linear infinite;
            box-shadow: 0 0 12px rgba(255, 0, 128, 0.5);
            transition: width 1s cubic-bezier(0.4, 0, 0.2, 1);
            width: 0%;
        }
        @keyframes rgbSlide {
            from { background-position: 0% 0; }
            to { background-position: 300% 0; }
        }

        .progress-info {
            display: flex;
            justify-content: space-between;
            margin-top: 10px;
            font-size: 11px;
            font-weight: 600;
            color: var(--text-muted);
            gap: 6px;
            flex-wrap: wrap;
        }
        .progress-info .highlight {
            color: var(--accent-bright);
            font-weight: 700;
        }

        /* ==================== Time Remaining Box ==================== */
        .time-remaining {
            margin-top: 12px;
            font-size: 13px;
            font-weight: 700;
            color: var(--accent-bright);
            text-align: center;
            padding: 10px 12px;
            background: var(--accent-soft);
            border: 1px solid var(--accent-border);
            border-radius: 10px;
        }

        /* ==================== Expiry Date Box ==================== */
        .expiry-box {
            margin-top: 10px;
            font-size: 12px;
            font-weight: 600;
            color: var(--text-secondary);
            text-align: center;
            padding: 8px 12px;
            background: rgba(255, 255, 255, 0.03);
            border: 1px solid var(--border);
            border-radius: 10px;
        }
        html.light .expiry-box {
            background: rgba(0, 0, 0, 0.03);
        }
        .expiry-box strong {
            color: var(--accent-bright);
            font-weight: 700;
        }

        /* ==================== Footer ==================== */
        .footer {
            text-align: center;
            margin-top: 32px;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        .footer .blink-text {
            font-weight: 900;
            font-size: 15px;
            letter-spacing: 1.5px;
        }

        html.light .footer .blink-text {
            animation: redBlink 1.2s ease-in-out infinite;
        }
        @keyframes redBlink {
            0%, 100% {
                color: #dc2626;
                text-shadow: 0 0 14px rgba(220, 38, 38, 0.8);
            }
            50% {
                color: #ff6b35;
                text-shadow: 0 0 14px rgba(255, 107, 53, 0.8);
            }
        }

        html:not(.light) .footer .blink-text {
            animation: blueBlink 1.2s ease-in-out infinite;
        }
        @keyframes blueBlink {
            0%, 100% {
                color: #3b82f6;
                text-shadow: 0 0 14px rgba(59, 130, 246, 0.8);
            }
            50% {
                color: #00d4ff;
                text-shadow: 0 0 14px rgba(0, 212, 255, 0.8);
            }
        }

        /* ==================== Animations ==================== */
        @keyframes fadeInUp {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }
        .animate { animation: fadeInUp 0.5s ease forwards; opacity: 0; }
        .delay-1 { animation-delay: 0.08s; }
        .delay-2 { animation-delay: 0.16s; }
        .delay-3 { animation-delay: 0.24s; }
        .delay-4 { animation-delay: 0.32s; }

        /* ==================== Responsive ==================== */
        @media (max-width: 600px) {
            .top-controls { top: 12px; left: 12px; }
            .ctrl-btn { padding: 8px 10px; font-size: 11px; }
            .container { padding: 90px 12px 16px; }
            .cards-grid { grid-template-columns: 1fr; gap: 14px; }
            .card { padding: 20px 18px; }
            .card-value { font-size: 26px; }
            .brand-name { font-size: 22px; }
            .logo-ring { width: 80px; height: 80px; }
            .logo-mark svg { width: 36px; height: 36px; }
        }
    </style>
</head>
<body>

    <!-- ==================== Top Controls ==================== -->
    <div class="top-controls">
        <button class="ctrl-btn" onclick="toggleLang()" title="Change Language">
            <span id="lang-flag">🇮🇷</span>
            <span id="lang-text">FA</span>
        </button>

        <button class="ctrl-btn" onclick="toggleTheme()" title="Toggle Theme">
            <span id="theme-icon">🌙</span>
        </button>

        <a class="ctrl-btn support-btn" href="https://t.me/vaslplus_support1" target="_blank" title="پشتیبانی / Support">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round">
                <path d="M3 18v-6a9 9 0 0 1 18 0v6"/>
                <path d="M21 19a2 2 0 0 1-2 2h-1a2 2 0 0 1-2-2v-3a2 2 0 0 1 2-2h3zM3 19a2 2 0 0 0 2 2h1a2 2 0 0 0 2-2v-3a2 2 0 0 0-2-2H3z"/>
            </svg>
        </a>
    </div>

    <div class="container">

        <!-- ==================== Logo ==================== -->
        <div class="logo-header animate delay-1">
            <div class="logo-ring">
                <div class="logo-mark">
                    <svg viewBox="0 0 24 24" fill="none">
                        <path d="M4 4 L10 20 L14 20 L20 4 L16 4 L12 15 L8 4 Z" fill="url(#silverGrad)"/>
                        <path class="logo-swoosh" d="M3 8 Q12 16 21 6" stroke-width="2" fill="none" stroke-linecap="round"/>
                        <defs>
                            <linearGradient id="silverGrad" x1="0%" y1="0%" x2="100%" y2="100%">
                                <stop offset="0%" stop-color="#ffffff"/>
                                <stop offset="100%" stop-color="#b8b8c8"/>
                            </linearGradient>
                        </defs>
                    </svg>
                </div>
            </div>
            <div class="brand-name">
                <span class="vasl">Vasl</span><span class="plus">Plus</span>
            </div>
            <div class="brand-tagline">FAST <span>•</span> SECURE <span>•</span> GLOBAL</div>
        </div>

        <!-- ==================== Greeting ==================== -->
        <div class="user-greeting animate delay-2">
            <span data-i18n="greeting">خوش آمدی،</span> <strong>__USER_NAME__</strong>
        </div>

        <!-- ==================== Cards ==================== -->
        <div class="cards-grid">

            <!-- کارت حجم مصرفی -->
            <div class="card-rgb animate delay-3">
                <div class="card">
                    <div class="card-icon">
                        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                            <path d="M4 7v10c0 2 1.5 3 3.5 3h9c2 0 3.5-1 3.5-3V7c0-2-1.5-3-3.5-3h-9C5.5 4 4 5 4 7z"/>
                            <path d="M8 12h8"/>
                        </svg>
                    </div>
                    <div class="card-label" data-i18n="used_label">حجم مصرف‌شده</div>
                    <div class="card-value" id="usage-value-container"></div>
                    <div class="rgb-progress">
                        <div class="rgb-progress-fill" id="usage-progress"></div>
                    </div>
                    <div class="progress-info">
                        <span><span class="highlight" id="used-percent">0</span><span data-i18n="percent_used">٪ مصرف شده</span></span>
                        <span><span data-i18n="remaining">باقی‌مانده:</span> <span class="highlight" id="remaining-gb">0</span> <span data-i18n="gb_unit">GB</span></span>
                    </div>
                </div>
            </div>

            <!-- کارت روزهای باقی‌مانده -->
            <div class="card-rgb animate delay-4">
                <div class="card">
                    <div class="card-icon">
                        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                            <rect x="3" y="4" width="18" height="18" rx="2" ry="2"/>
                            <line x1="16" y1="2" x2="16" y2="6"/>
                            <line x1="8" y1="2" x2="8" y2="6"/>
                            <line x1="3" y1="10" x2="21" y2="10"/>
                        </svg>
                    </div>
                    <div class="card-label" data-i18n="days_label">روزهای باقی‌مانده</div>
                    <div class="card-value" id="days-value-container"></div>
                    <div class="rgb-progress">
                        <div class="rgb-progress-fill" id="days-progress"></div>
                    </div>

                    <!-- زمان دقیق باقی‌مانده -->
                    <div class="time-remaining" id="time-remaining">—</div>

                    <!-- تاریخ انقضا -->
                    <div class="expiry-box">
                        <span data-i18n="expiry_label">تاریخ انقضا:</span>
                        <strong id="expiry-date">—</strong>
                    </div>
                </div>
            </div>

        </div>

        <!-- ==================== Footer ==================== -->
        <div class="footer animate delay-4">
            <span class="blink-text" id="footer-text">پنل کاربری وصل پلاس</span>
        </div>

    </div>

    <script>
        // ==================== Data ====================
        const DATA = {
            userName: "__USER_NAME__",
            userId: "__USER_ID__",
            statusCode: "__STATUS_CODE__",
            totalGb: "__TOTAL_GB__",
            limitTotalGb: "__LIMIT_TOTAL_GB__",
            totalPercent: "__TOTAL_PERCENT__",
            dailyGb: "__DAILY_GB__",
            limitDailyGb: "__LIMIT_DAILY_GB__",
            dailyPercent: "__DAILY_PERCENT__",
            expiryDate: "__EXPIRY_DATE__"
        };

        const isUnlimitedGB = DATA.limitTotalGb === "9999" ||
                              DATA.limitTotalGb === "Unlimited" ||
                              parseFloat(DATA.limitTotalGb) >= 9999;

        const isUnlimitedDays = !DATA.expiryDate ||
                                DATA.expiryDate === "2099-01-01" ||
                                DATA.expiryDate === "Never Expire";

        // ==================== i18n ====================
        const I18N = {
            fa: {
                greeting: "خوش آمدی،",
                used_label: "حجم مصرف‌شده",
                days_label: "روزهای باقی‌مانده",
                days_unit: "روز",
                percent_used: "٪ مصرف شده",
                remaining: "باقی‌مانده:",
                expiry_label: "تاریخ انقضا:",
                unlimited: "نامحدود",
                gb_unit: "GB",
                expired: "منقضی شده",
                no_expiry: "بدون انقضا",
                footer: "پنل کاربری وصل پلاس",
                day: "روز",
                days: "روز",
                hour: "ساعت",
                hours: "ساعت",
                minute: "دقیقه",
                minutes: "دقیقه",
                and: "و",
                expired_text: "منقضی شده",
                remaining_prefix: "باقی‌مانده"
            },
            en: {
                greeting: "Welcome,",
                used_label: "Data Used",
                days_label: "Days Remaining",
                days_unit: "days",
                percent_used: "% used",
                remaining: "Remaining:",
                expiry_label: "Expiry date:",
                unlimited: "Unlimited",
                gb_unit: "GB",
                expired: "Expired",
                no_expiry: "No expiry",
                footer: "VaslPlus User Panel",
                day: "day",
                days: "days",
                hour: "hour",
                hours: "hours",
                minute: "minute",
                minutes: "minutes",
                and: "and",
                expired_text: "Expired",
                remaining_prefix: "remaining"
            }
        };

        let currentLang = localStorage.getItem("vp_lang") || "fa";
        let currentTheme = localStorage.getItem("vp_theme") || "dark";

        const $ = (id) => document.getElementById(id);

        // ==================== Format Number ====================
        function formatNumber(num, maxDecimals = 2) {
            if (!isFinite(num) || num === 0) return "0";
            const rounded = Math.round(num * Math.pow(10, maxDecimals)) / Math.pow(10, maxDecimals);
            if (rounded === Math.floor(rounded)) return rounded.toString();
            return parseFloat(rounded.toFixed(maxDecimals)).toString();
        }

        // ==================== Time Breakdown ====================
        function getTimeRemaining(expiryStr) {
            if (!expiryStr || expiryStr === "Never Expire" || expiryStr === "2099-01-01") {
                return { valid: false, days: 0, hours: 0, minutes: 0, expired: false };
            }
            let expiry = null;
            if (/^\d{4}-\d{1,2}-\d{1,2}/.test(expiryStr)) {
                expiry = new Date(expiryStr);
            } else if (/^\d+$/.test(expiryStr)) {
                const num = parseInt(expiryStr);
                expiry = new Date(num > 10000000000 ? num : num * 1000);
            } else {
                expiry = new Date(expiryStr);
            }
            if (!expiry || isNaN(expiry.getTime())) {
                return { valid: false, days: 0, hours: 0, minutes: 0, expired: false };
            }

            const now = new Date();
            const diffMs = expiry.getTime() - now.getTime();

            if (diffMs <= 0) {
                return { valid: true, days: 0, hours: 0, minutes: 0, expired: true };
            }

            const totalMinutes = Math.floor(diffMs / (1000 * 60));
            const days = Math.floor(totalMinutes / (60 * 24));
            const hours = Math.floor((totalMinutes % (60 * 24)) / 60);
            const minutes = totalMinutes % 60;

            return { valid: true, days, hours, minutes, expired: false };
        }

        // ==================== Format Time Text ====================
        function formatTimeText(timeData, lang) {
            const t = I18N[lang];

            if (timeData.expired) {
                return t.expired_text;
            }

            const parts = [];
            if (timeData.days > 0) {
                parts.push(`${timeData.days} ${timeData.days === 1 ? t.day : t.days}`);
            }
            if (timeData.hours > 0) {
                parts.push(`${timeData.hours} ${timeData.hours === 1 ? t.hour : t.hours}`);
            }
            if (timeData.days === 0 && timeData.minutes > 0) {
                parts.push(`${timeData.minutes} ${timeData.minutes === 1 ? t.minute : t.minutes}`);
            }

            if (parts.length === 0) {
                return lang === "fa" ? "کمتر از یک دقیقه" : "less than a minute";
            }

            const joined = parts.join(` ${t.and} `);
            return lang === "fa" ? `${joined} ${t.remaining_prefix}` : `${joined} ${t.remaining_prefix}`;
        }

        // ==================== Calculate Days Progress ====================
        function calcDaysPercent(timeData) {
            if (timeData.expired) return 0;
            const totalMinutes = 30 * 24 * 60;
            const remainingMinutes = (timeData.days * 24 * 60) + (timeData.hours * 60) + timeData.minutes;
            if (remainingMinutes >= totalMinutes) return 100;
            return Math.max(0, Math.min(100, (remainingMinutes / totalMinutes) * 100));
        }

        // ==================== Locale Date ====================
        function toLocaleDate(dateStr, lang) {
            if (!dateStr || dateStr === "Never Expire" || dateStr === "2099-01-01") return "—";
            try {
                const d = new Date(dateStr);
                if (isNaN(d.getTime())) return "—";
                const locale = lang === "fa" ? "fa-IR" : "en-GB";
                return d.toLocaleDateString(locale, {
                    year: 'numeric', month: '2-digit', day: '2-digit'
                });
            } catch (e) { return "—"; }
        }

        // ==================== Apply Language ====================
        function applyLang() {
            const t = I18N[currentLang];
            document.documentElement.lang = currentLang;
            document.documentElement.dir = currentLang === "fa" ? "rtl" : "ltr";

            document.querySelectorAll("[data-i18n]").forEach(el => {
                const key = el.getAttribute("data-i18n");
                if (t[key]) el.textContent = t[key];
            });

            $("lang-flag").textContent = currentLang === "fa" ? "🇮🇷" : "🇬🇧";
            $("lang-text").textContent = currentLang === "fa" ? "FA" : "EN";
            $("footer-text").textContent = t.footer;

            render();
        }

        function toggleLang() {
            currentLang = currentLang === "fa" ? "en" : "fa";
            localStorage.setItem("vp_lang", currentLang);
            applyLang();
        }

        // ==================== Apply Theme ====================
        function applyTheme() {
            document.documentElement.classList.toggle("light", currentTheme === "light");
            $("theme-icon").textContent = currentTheme === "dark" ? "🌙" : "☀️";
        }

        function toggleTheme() {
            currentTheme = currentTheme === "dark" ? "light" : "dark";
            localStorage.setItem("vp_theme", currentTheme);
            applyTheme();
        }

        // ==================== Render ====================
        function render() {
            const t = I18N[currentLang];
            const usageContainer = $("usage-value-container");
            const daysContainer = $("days-value-container");

            // ========== Usage ==========
            if (isUnlimitedGB) {
                usageContainer.innerHTML = `
                    <span class="infinity">∞</span>
                    <span class="infinity-text">${t.unlimited}</span>
                `;
                setTimeout(() => {
                    $("usage-progress").style.width = "100%";
                }, 300);
            } else {
                const used = parseFloat(DATA.totalGb) || 0;
                const total = parseFloat(DATA.limitTotalGb) || 0;
                const remaining = Math.max(0, total - used);
                const percent = parseFloat(DATA.totalPercent) || 0;

                usageContainer.innerHTML = `
                    <span class="num">${formatNumber(used)}</span>
                    <span class="unit">/</span>
                    <span class="num">${formatNumber(total)}</span>
                    <span class="unit unit-accent">GB</span>
                `;

                $("used-percent").textContent = formatNumber(percent, 1);
                $("remaining-gb").textContent = formatNumber(remaining);

                setTimeout(() => {
                    $("usage-progress").style.width = Math.min(100, percent) + "%";
                }, 300);
            }

            // ========== Days ==========
            if (isUnlimitedDays) {
                daysContainer.innerHTML = `
                    <span class="infinity">∞</span>
                    <span class="infinity-text">${t.unlimited}</span>
                `;
                $("days-progress").style.width = "100%";
                $("time-remaining").textContent = t.no_expiry;
                $("expiry-date").textContent = t.no_expiry;
            } else {
                const timeData = getTimeRemaining(DATA.expiryDate);

                if (!timeData.valid) {
                    daysContainer.innerHTML = `
                        <span class="num">0</span>
                        <span class="unit unit-accent">${t.expired}</span>
                    `;
                    $("days-progress").style.width = "0%";
                    $("time-remaining").textContent = t.expired_text;
                    $("expiry-date").textContent = "—";
                } else if (timeData.expired) {
                    daysContainer.innerHTML = `
                        <span class="num">0</span>
                        <span class="unit unit-accent">${t.expired}</span>
                    `;
                    $("days-progress").style.width = "0%";
                    $("time-remaining").textContent = t.expired_text;
                    $("expiry-date").textContent = toLocaleDate(DATA.expiryDate, currentLang);
                } else {
                    daysContainer.innerHTML = `
                        <span class="num">${timeData.days}</span>
                        <span class="unit unit-accent">${t.days_unit}</span>
                    `;

                    const remainingPercent = calcDaysPercent(timeData);

                    $("time-remaining").textContent = formatTimeText(timeData, currentLang);
                    $("expiry-date").textContent = toLocaleDate(DATA.expiryDate, currentLang);

                    setTimeout(() => {
                        $("days-progress").style.width = remainingPercent + "%";
                    }, 300);
                }
            }
        }

        // ==================== Init ====================
        (function init() {
            applyTheme();
            applyLang();
        })();
    </script>

</body>
</html>
