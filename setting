<!DOCTYPE html>
<html lang="fa" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>VaslPlus | __USER_NAME__</title>
    <link href="https://fonts.googleapis.com/css2?family=Vazirmatn:wght@400;500;600;700;800;900&display=swap" rel="stylesheet">
    <style>
        /* ==================== Root Variables ==================== */
        :root {
            --accent: #ff2338;
            --accent-bright: #ff4d5e;
            --accent-glow: rgba(255, 35, 56, 0.4);
            --accent-soft: rgba(255, 35, 56, 0.12);
            --accent-border: rgba(255, 35, 56, 0.35);
            --bg-primary: #0a0a0f;
            --bg-card: rgba(20, 20, 28, 0.7);
            --text-primary: #ffffff;
            --text-muted: #6b6b80;
            --text-secondary: #a0a0b0;
            --border: #23232f;
            --ring-bg: #23232f;
            --gradient-accent: linear-gradient(135deg, #ff2338 0%, #ff4d5e 100%);
            --gradient-silver: linear-gradient(135deg, #ffffff 0%, #b8b8c8 100%);
            --fib1: #ff2338;
            --fib2: #ff6b35;
            --fib3: #ffd700;
            --footer-glow: rgba(255, 35, 56, 0.5);
        }

        html.light {
            --accent: #0d6efd;
            --accent-bright: #3b82f6;
            --accent-glow: rgba(13, 110, 253, 0.35);
            --accent-soft: rgba(13, 110, 253, 0.12);
            --accent-border: rgba(13, 110, 253, 0.35);
            --bg-primary: #f0f6ff;
            --bg-card: rgba(255, 255, 255, 0.75);
            --text-primary: #0a2540;
            --text-muted: #94a3b8;
            --text-secondary: #475569;
            --border: #dbeafe;
            --ring-bg: #dbeafe;
            --gradient-accent: linear-gradient(135deg, #0d6efd 0%, #3b82f6 100%);
            --gradient-silver: linear-gradient(135deg, #0a2540 0%, #1e40af 100%);
            --footer-glow: rgba(13, 110, 253, 0.5);
        }

        * { margin: 0; padding: 0; box-sizing: border-box; }

        html, body {
            font-family: 'Vazirmatn', sans-serif;
            background: var(--bg-primary);
            color: var(--text-primary);
            min-height: 100vh;
            overflow-x: hidden;
            transition: background 0.5s, color 0.5s;
        }

        /* ==================== Fiber Optic Background ==================== */
        .fiber-bg {
            position: fixed;
            inset: 0;
            z-index: 0;
            overflow: hidden;
            pointer-events: none;
        }

        .fiber-bg svg {
            width: 100%;
            height: 100%;
            display: block;
        }

        .fiber-line {
            fill: none;
            stroke-width: 1.5;
            stroke-linecap: round;
            filter: drop-shadow(0 0 4px currentColor);
        }

        .fiber-line.f1 { stroke: var(--fib1); animation: fiberPulse1 4s ease-in-out infinite; }
        .fiber-line.f2 { stroke: var(--fib2); animation: fiberPulse2 5s ease-in-out infinite; }
        .fiber-line.f3 { stroke: var(--fib3); animation: fiberPulse3 6s ease-in-out infinite; }
        .fiber-line.f4 { stroke: var(--fib1); animation: fiberPulse1 5.5s ease-in-out infinite 0.5s; }
        .fiber-line.f5 { stroke: var(--fib2); animation: fiberPulse2 4.5s ease-in-out infinite 1s; }
        .fiber-line.f6 { stroke: var(--fib3); animation: fiberPulse3 5.5s ease-in-out infinite 1.5s; }

        @keyframes fiberPulse1 {
            0%, 100% { opacity: 0.15; stroke-width: 1.5; }
            50% { opacity: 0.75; stroke-width: 2.5; }
        }
        @keyframes fiberPulse2 {
            0%, 100% { opacity: 0.2; stroke-width: 1.5; }
            50% { opacity: 0.7; stroke-width: 2.5; }
        }
        @keyframes fiberPulse3 {
            0%, 100% { opacity: 0.18; stroke-width: 1.5; }
            50% { opacity: 0.65; stroke-width: 2.5; }
        }

        .fiber-particle {
            fill: var(--fib1);
            filter: drop-shadow(0 0 6px currentColor);
        }
        .fiber-particle.p2 { fill: var(--fib2); }
        .fiber-particle.p3 { fill: var(--fib3); }

        /* ==================== Top Controls ==================== */
        .top-controls {
            position: fixed;
            top: 20px;
            left: 20px;
            display: flex;
            gap: 10px;
            z-index: 100;
        }

        .ctrl-btn {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 6px;
            padding: 9px 14px;
            background: var(--bg-card);
            backdrop-filter: blur(12px);
            -webkit-backdrop-filter: blur(12px);
            border: 1px solid var(--border);
            border-radius: 10px;
            color: var(--text-secondary);
            font-family: inherit;
            font-size: 12px;
            font-weight: 700;
            cursor: pointer;
            transition: all 0.3s;
        }
        .ctrl-btn:hover {
            border-color: var(--accent);
            color: var(--accent-bright);
            transform: translateY(-2px);
            box-shadow: 0 8px 24px var(--accent-glow);
        }
        .ctrl-btn svg { width: 18px; height: 18px; }
        .ctrl-btn.support-btn {
            background: linear-gradient(135deg, #0088cc 0%, #00a8e8 100%);
            border-color: #0088cc;
            color: #ffffff;
            padding: 9px 12px;
        }
        .ctrl-btn.support-btn:hover {
            box-shadow: 0 8px 24px rgba(0, 136, 204, 0.5);
            border-color: #00a8e8;
        }

        /* ==================== Container ==================== */
        .container {
            position: relative;
            z-index: 1;
            width: 100%;
            max-width: 640px;
            margin: 0 auto;
            padding: 100px 16px 24px;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
        }

        /* ==================== Logo with RGB Ring ==================== */
        .logo-header { text-align: center; margin-bottom: 36px; }

        .logo-ring {
            position: relative;
            width: 92px;
            height: 92px;
            margin: 0 auto 16px;
            border-radius: 50%;
            padding: 3px;
            background: conic-gradient(
                from 0deg,
                #ff0000,
                #ff8000,
                #ffff00,
                #00ff00,
                #00ffff,
                #0080ff,
                #8000ff,
                #ff00ff,
                #ff0000
            );
            animation: rgbRotate 4s linear infinite;
            box-shadow: 0 0 25px rgba(255, 0, 128, 0.3),
                        0 0 50px rgba(0, 128, 255, 0.2);
        }
        @keyframes rgbRotate {
            from { filter: hue-rotate(0deg); }
            to { filter: hue-rotate(360deg); }
        }

        .logo-mark {
            width: 100%;
            height: 100%;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            background: var(--bg-primary);
            position: relative;
        }
        .logo-mark svg { width: 44px; height: 44px; }

        .brand-name {
            font-size: 26px;
            font-weight: 900;
            letter-spacing: 0.5px;
            line-height: 1;
            direction: ltr;
        }
        .brand-name .vasl {
            background: var(--gradient-silver);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }
        .brand-name .plus {
            color: var(--accent-bright);
            text-shadow: 0 0 20px var(--accent-glow);
        }
        .brand-tagline {
            font-size: 10px;
            font-weight: 600;
            letter-spacing: 3px;
            color: var(--text-muted);
            margin-top: 6px;
            direction: ltr;
        }
        .brand-tagline span { color: var(--accent); margin: 0 6px; }

        /* ==================== User Greeting ==================== */
        .user-greeting {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
            margin-bottom: 24px;
            font-size: 14px;
            color: var(--text-secondary);
        }
        .user-greeting strong { color: var(--text-primary); font-weight: 700; }

        /* ==================== Cards ==================== */
        .cards-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 16px;
        }
        .card {
            background: var(--bg-card);
            backdrop-filter: blur(12px);
            -webkit-backdrop-filter: blur(12px);
            border: 1px solid var(--border);
            border-radius: 16px;
            padding: 24px 22px;
            position: relative;
            overflow: hidden;
            transition: all 0.3s;
        }
        .card::before {
            content: '';
            position: absolute;
            top: 0; left: 0; right: 0;
            height: 3px;
            background: linear-gradient(90deg, #ff0000, #ff8000, #ffff00, #00ff00, #00ffff, #0080ff, #8000ff, #ff00ff, #ff0000);
            background-size: 200% 100%;
            animation: rgbSlide 3s linear infinite;
        }
        @keyframes rgbSlide {
            from { background-position: 0% 0; }
            to { background-position: 200% 0; }
        }
        .card:hover {
            border-color: var(--accent-border);
            transform: translateY(-4px);
            box-shadow: 0 8px 32px var(--accent-glow);
        }
        .card-icon {
            width: 42px;
            height: 42px;
            border-radius: 10px;
            display: flex;
            align-items: center;
            justify-content: center;
            background: var(--accent-soft);
            border: 1px solid var(--accent-border);
            color: var(--accent-bright);
            margin-bottom: 16px;
        }
        .card-icon svg { width: 20px; height: 20px; }
        .card-label {
            font-size: 12px;
            font-weight: 600;
            color: var(--text-secondary);
            margin-bottom: 10px;
        }
        .card-value {
            font-size: 30px;
            font-weight: 900;
            line-height: 1;
            display: flex;
            align-items: baseline;
            gap: 5px;
            direction: ltr;
            flex-wrap: wrap;
        }
        .card-value .num { color: var(--text-primary); }
        .card-value .unit { font-size: 13px; font-weight: 600; color: var(--text-muted); }
        .card-value .unit-accent { color: var(--accent-bright); }
        .card-value .infinity {
            font-size: 42px;
            font-weight: 900;
            color: var(--accent-bright);
            text-shadow: 0 0 20px var(--accent-glow);
            line-height: 0.8;
        }
        .card-value .infinity-text {
            font-size: 18px;
            font-weight: 700;
            color: var(--accent-bright);
            margin-right: 6px;
        }

        /* ==================== RGB Progress Bar ==================== */
        .rgb-progress {
            position: relative;
            height: 8px;
            border-radius: 8px;
            background: var(--ring-bg);
            overflow: hidden;
            margin-top: 16px;
        }
        .rgb-progress-fill {
            height: 100%;
            border-radius: 8px;
            background: linear-gradient(90deg, #ff0000, #ff8000, #ffff00, #00ff00, #00ffff, #0080ff, #8000ff, #ff00ff, #ff0000);
            background-size: 300% 100%;
            animation: rgbSlide 3s linear infinite;
            box-shadow: 0 0 12px rgba(255, 0, 128, 0.5);
            transition: width 1s cubic-bezier(0.4, 0, 0.2, 1);
            position: relative;
        }
        .rgb-progress-fill::after {
            content: '';
            position: absolute;
            inset: 0;
            background: linear-gradient(90deg, transparent, rgba(255,255,255,0.5), transparent);
            animation: shimmer 2s infinite;
        }
        @keyframes shimmer {
            0% { transform: translateX(-100%); }
            100% { transform: translateX(100%); }
        }

        .progress-info {
            display: flex;
            justify-content: space-between;
            margin-top: 10px;
            font-size: 11px;
            font-weight: 600;
            color: var(--text-muted);
            gap: 8px;
            flex-wrap: wrap;
        }
        .progress-info .highlight {
            color: var(--accent-bright);
            font-weight: 700;
        }

        /* ==================== Days Ring (RGB) ==================== */
        .days-ring {
            margin-top: 16px;
            display: flex;
            gap: 4px;
        }
        .day-seg {
            flex: 1;
            height: 6px;
            border-radius: 4px;
            background: var(--ring-bg);
            transition: all 0.4s;
        }
        .day-seg.active {
            background: linear-gradient(90deg, #ff0000, #ff8000, #ffff00, #00ff00, #00ffff, #0080ff, #8000ff, #ff00ff);
            background-size: 300% 100%;
            animation: rgbSlide 4s linear infinite;
            box-shadow: 0 0 8px rgba(255, 100, 0, 0.5);
        }

        /* ==================== Footer with Blink ==================== */
        .footer {
            text-align: center;
            margin-top: 32px;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        .footer .blink-text {
            font-weight: 900;
            font-size: 15px;
            letter-spacing: 1.5px;
            transition: color 0.3s;
        }

        /* روز: قرمز با فلاشر */
        html.light .footer .blink-text {
            animation: redBlink 1.2s ease-in-out infinite;
        }
        @keyframes redBlink {
            0%, 100% {
                color: #dc2626;
                text-shadow: 0 0 14px rgba(220, 38, 38, 0.8),
                             0 0 28px rgba(220, 38, 38, 0.4);
            }
            50% {
                color: #ff6b35;
                text-shadow: 0 0 14px rgba(255, 107, 53, 0.8),
                             0 0 28px rgba(255, 107, 53, 0.4);
            }
        }

        /* شب: آبی با فلاشر */
        html:not(.light) .footer .blink-text {
            animation: blueBlink 1.2s ease-in-out infinite;
        }
        @keyframes blueBlink {
            0%, 100% {
                color: #3b82f6;
                text-shadow: 0 0 14px rgba(59, 130, 246, 0.8),
                             0 0 28px rgba(59, 130, 246, 0.4);
            }
            50% {
                color: #00d4ff;
                text-shadow: 0 0 14px rgba(0, 212, 255, 0.8),
                             0 0 28px rgba(0, 212, 255, 0.4);
            }
        }

        /* ==================== Animations ==================== */
        @keyframes fadeInUp {
            from { opacity: 0; transform: translateY(24px); }
            to { opacity: 1; transform: translateY(0); }
        }
        .animate { animation: fadeInUp 0.6s ease forwards; opacity: 0; }
        .delay-1 { animation-delay: 0.1s; }
        .delay-2 { animation-delay: 0.2s; }
        .delay-3 { animation-delay: 0.3s; }
        .delay-4 { animation-delay: 0.4s; }

        /* ==================== Responsive ==================== */
        @media (max-width: 600px) {
            .top-controls { top: 12px; left: 12px; }
            .ctrl-btn { padding: 8px 10px; font-size: 11px; }
            .container { padding: 90px 12px 16px; }
            .cards-grid { grid-template-columns: 1fr; gap: 14px; }
            .card { padding: 22px 20px; }
            .card-value { font-size: 28px; }
            .brand-name { font-size: 22px; }
            .logo-ring { width: 80px; height: 80px; }
            .logo-mark svg { width: 36px; height: 36px; }
        }
    </style>
</head>
<body>

    <!-- ==================== Fiber Optic Background ==================== -->
    <div class="fiber-bg">
        <svg viewBox="0 0 1200 800" preserveAspectRatio="none">
            <defs>
                <radialGradient id="glowRed" cx="50%" cy="50%" r="50%">
                    <stop offset="0%" stop-color="#ff2338" stop-opacity="0.8"/>
                    <stop offset="100%" stop-color="#ff2338" stop-opacity="0"/>
                </radialGradient>
                <radialGradient id="glowOrange" cx="50%" cy="50%" r="50%">
                    <stop offset="0%" stop-color="#ff6b35" stop-opacity="0.8"/>
                    <stop offset="100%" stop-color="#ff6b35" stop-opacity="0"/>
                </radialGradient>
                <radialGradient id="glowGold" cx="50%" cy="50%" r="50%">
                    <stop offset="0%" stop-color="#ffd700" stop-opacity="0.8"/>
                    <stop offset="100%" stop-color="#ffd700" stop-opacity="0"/>
                </radialGradient>
            </defs>

            <!-- Fiber Lines -->
            <path class="fiber-line f1" d="M-100,100 Q200,150 400,100 T900,200 T1300,150"/>
            <path class="fiber-line f2" d="M-100,250 Q150,200 350,280 T800,220 T1300,300"/>
            <path class="fiber-line f3" d="M-100,400 Q250,450 500,380 T950,450 T1300,400"/>
            <path class="fiber-line f4" d="M-100,550 Q200,500 450,580 T1000,520 T1300,600"/>
            <path class="fiber-line f5" d="M-100,700 Q300,650 550,720 T1050,660 T1300,700"/>
            <path class="fiber-line f6" d="M200,-50 Q300,200 250,400 T350,700 T300,850"/>

            <!-- Moving Particles -->
            <circle class="fiber-particle" r="3">
                <animateMotion dur="6s" repeatCount="indefinite" path="M-100,100 Q200,150 400,100 T900,200 T1300,150"/>
            </circle>
            <circle class="fiber-particle p2" r="3">
                <animateMotion dur="7s" repeatCount="indefinite" path="M-100,400 Q250,450 500,380 T950,450 T1300,400"/>
            </circle>
            <circle class="fiber-particle p3" r="3">
                <animateMotion dur="5s" repeatCount="indefinite" path="M-100,700 Q300,650 550,720 T1050,660 T1300,700"/>
            </circle>
            <circle class="fiber-particle" r="2.5">
                <animateMotion dur="8s" repeatCount="indefinite" path="M-100,250 Q150,200 350,280 T800,220 T1300,300"/>
            </circle>
            <circle class="fiber-particle p2" r="2.5">
                <animateMotion dur="6.5s" repeatCount="indefinite" path="M-100,550 Q200,500 450,580 T1000,520 T1300,600"/>
            </circle>
        </svg>
    </div>

    <!-- ==================== Top Controls ==================== -->
    <div class="top-controls">
        <!-- دکمه زبان -->
        <button class="ctrl-btn" onclick="toggleLang()" title="Change Language">
            <span id="lang-flag">🇮🇷</span>
            <span id="lang-text">FA</span>
        </button>

        <!-- دکمه تم -->
        <button class="ctrl-btn" onclick="toggleTheme()" title="Toggle Theme">
            <span id="theme-icon">🌙</span>
        </button>

        <!-- دکمه پشتیبانی -->
        <a class="ctrl-btn support-btn" href="https://t.me/vaslplus_support1" target="_blank" title="Support">
            <svg viewBox="0 0 24 24" fill="currentColor">
                <path d="M9.78 18.65l.28-4.23 7.68-6.92c.34-.31-.07-.46-.52-.19L7.74 13.3 3.64 12c-.88-.25-.89-.86.2-1.3l15.97-6.16c.73-.33 1.43.18 1.15 1.3l-2.72 12.81c-.19.91-.74 1.13-1.5.71L12.6 16.3l-1.99 1.93c-.23.23-.42.42-.83.42z"/>
            </svg>
        </a>
    </div>

    <div class="container">

        <!-- ==================== Logo ==================== -->
        <div class="logo-header animate delay-1">
            <div class="logo-ring">
                <div class="logo-mark">
                    <svg viewBox="0 0 24 24" fill="none">
                        <path d="M4 4 L10 20 L14 20 L20 4 L16 4 L12 15 L8 4 Z" fill="url(#silverGrad)"/>
                        <path class="logo-swoosh" d="M3 8 Q12 16 21 6" stroke="currentColor" stroke-width="2" fill="none" stroke-linecap="round"/>
                        <defs>
                            <linearGradient id="silverGrad" x1="0%" y1="0%" x2="100%" y2="100%">
                                <stop offset="0%" stop-color="#ffffff"/>
                                <stop offset="100%" stop-color="#b8b8c8"/>
                            </linearGradient>
                        </defs>
                    </svg>
                </div>
            </div>
            <div class="brand-name">
                <span class="vasl">Vasl</span><span class="plus">Plus</span>
            </div>
            <div class="brand-tagline">FAST <span>•</span> SECURE <span>•</span> GLOBAL</div>
        </div>

        <!-- ==================== Greeting ==================== -->
        <div class="user-greeting animate delay-2">
            <span data-i18n="greeting">خوش آمدی،</span> <strong>__USER_NAME__</strong>
        </div>

        <!-- ==================== Cards ==================== -->
        <div class="cards-grid">

            <!-- کارت حجم مصرفی -->
            <div class="card animate delay-3">
                <div class="card-icon">
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                        <path d="M4 7v10c0 2 1.5 3 3.5 3h9c2 0 3.5-1 3.5-3V7c0-2-1.5-3-3.5-3h-9C5.5 4 4 5 4 7z"/>
                        <path d="M8 12h8"/>
                    </svg>
                </div>
                <div class="card-label" data-i18n="used_label">حجم مصرف‌شده</div>
                <div class="card-value" id="usage-value-container"></div>
                <div class="rgb-progress">
                    <div class="rgb-progress-fill" id="usage-progress" style="width: 0%;"></div>
                </div>
                <div class="progress-info">
                    <span><span class="highlight" id="used-percent">0</span><span data-i18n="percent_used">٪ مصرف شده</span></span>
                    <span><span data-i18n="remaining">باقی‌مانده:</span> <span class="highlight" id="remaining-gb">0</span> <span data-i18n="gb_unit">GB</span></span>
                </div>
            </div>

            <!-- کارت روزهای باقی‌مانده -->
            <div class="card animate delay-4">
                <div class="card-icon">
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                        <rect x="3" y="4" width="18" height="18" rx="2" ry="2"/>
                        <line x1="16" y1="2" x2="16" y2="6"/>
                        <line x1="8" y1="2" x2="8" y2="6"/>
                        <line x1="3" y1="10" x2="21" y2="10"/>
                    </svg>
                </div>
                <div class="card-label" data-i18n="days_label">روزهای باقی‌مانده</div>
                <div class="card-value" id="days-value-container"></div>
                <div class="days-ring" id="days-ring"></div>
                <div class="progress-info" style="margin-top: 10px;">
                    <span data-i18n="expiry_label">تاریخ انقضا:</span>
                    <span class="highlight" id="expiry-date">—</span>
                </div>
            </div>

        </div>

        <!-- ==================== Footer ==================== -->
        <div class="footer animate delay-4">
            <span class="blink-text" id="footer-text">پنل کاربری وصل پلاس</span>
        </div>

    </div>

    <script>
        // ==================== Data ====================
        const DATA = {
            userName: "__USER_NAME__",
            userId: "__USER_ID__",
            statusCode: "__STATUS_CODE__",
            totalGb: "__TOTAL_GB__",
            limitTotalGb: "__LIMIT_TOTAL_GB__",
            totalPercent: "__TOTAL_PERCENT__",
            dailyGb: "__DAILY_GB__",
            limitDailyGb: "__LIMIT_DAILY_GB__",
            dailyPercent: "__DAILY_PERCENT__",
            expiryDate: "__EXPIRY_DATE__"
        };

        const isUnlimitedGB = DATA.limitTotalGb === "9999" ||
                              DATA.limitTotalGb === "Unlimited" ||
                              parseFloat(DATA.limitTotalGb) >= 9999;

        const isUnlimitedDays = !DATA.expiryDate ||
                                DATA.expiryDate === "2099-01-01" ||
                                DATA.expiryDate === "Never Expire";

        // ==================== i18n ====================
        const I18N = {
            fa: {
                greeting: "خوش آمدی،",
                used_label: "حجم مصرف‌شده",
                days_label: "روزهای باقی‌مانده",
                days_unit: "روز",
                percent_used: "٪ مصرف شده",
                remaining: "باقی‌مانده:",
                expiry_label: "تاریخ انقضا:",
                unlimited: "نامحدود",
                gb_unit: "GB",
                expired: "منقضی شده",
                no_expiry: "بدون انقضا",
                footer: "پنل کاربری وصل پلاس"
            },
            en: {
                greeting: "Welcome,",
                used_label: "Data Used",
                days_label: "Days Remaining",
                days_unit: "days",
                percent_used: "% used",
                remaining: "Remaining:",
                expiry_label: "Expiry date:",
                unlimited: "Unlimited",
                gb_unit: "GB",
                expired: "Expired",
                no_expiry: "No expiry",
                footer: "VaslPlus User Panel"
            }
        };

        let currentLang = localStorage.getItem("vp_lang") || "fa";
        let currentTheme = localStorage.getItem("vp_theme") || "dark";

        const $ = (id) => document.getElementById(id);

        // ==================== Format Number ====================
        function formatNumber(num, maxDecimals = 2) {
            if (!isFinite(num) || num === 0) return "0";
            const rounded = Math.round(num * Math.pow(10, maxDecimals)) / Math.pow(10, maxDecimals);
            if (rounded === Math.floor(rounded)) return rounded.toString();
            return parseFloat(rounded.toFixed(maxDecimals)).toString();
        }

        // ==================== Days Left ====================
        function calcDaysLeft(expiryStr) {
            if (!expiryStr || expiryStr === "Never Expire" || expiryStr === "2099-01-01") {
                return { days: 0, expired: false, valid: false };
            }
            let expiry = null;
            if (/^\d{4}-\d{1,2}-\d{1,2}/.test(expiryStr)) {
                expiry = new Date(expiryStr);
            } else if (/^\d+$/.test(expiryStr)) {
                const num = parseInt(expiryStr);
                expiry = new Date(num > 10000000000 ? num : num * 1000);
            } else {
                expiry = new Date(expiryStr);
            }
            if (!expiry || isNaN(expiry.getTime())) {
                return { days: 0, expired: false, valid: false };
            }
            const now = new Date();
            const diffMs = expiry.getTime() - now.getTime();
            if (diffMs <= 0) return { days: 0, expired: true, valid: true };
            const totalHours = diffMs / (1000 * 60 * 60);
            const fullDays = Math.floor(totalHours / 24);
            const remainingHours = totalHours % 24;
            const days = remainingHours > 0 ? fullDays + 1 : fullDays;
            return { days: Math.max(0, days), expired: false, valid: true };
        }

        // ==================== Locale Date ====================
        function toLocaleDate(dateStr, lang) {
            if (!dateStr || dateStr === "Never Expire" || dateStr === "2099-01-01") return "—";
            try {
                const d = new Date(dateStr);
                if (isNaN(d.getTime())) return "—";
                const locale = lang === "fa" ? "fa-IR" : "en-GB";
                return d.toLocaleDateString(locale, {
                    year: 'numeric', month: '2-digit', day: '2-digit'
                });
            } catch (e) { return "—"; }
        }

        // ==================== Apply Language ====================
        function applyLang() {
            const t = I18N[currentLang];
            document.documentElement.lang = currentLang;
            document.documentElement.dir = currentLang === "fa" ? "rtl" : "ltr";

            document.querySelectorAll("[data-i18n]").forEach(el => {
                const key = el.getAttribute("data-i18n");
                if (t[key]) el.textContent = t[key];
            });

            $("lang-flag").textContent = currentLang === "fa" ? "🇮🇷" : "🇬🇧";
            $("lang-text").textContent = currentLang === "fa" ? "FA" : "EN";
            $("footer-text").textContent = t.footer;

            render();
        }

        function toggleLang() {
            currentLang = currentLang === "fa" ? "en" : "fa";
            localStorage.setItem("vp_lang", currentLang);
            applyLang();
        }

        // ==================== Apply Theme ====================
        function applyTheme() {
            document.documentElement.classList.toggle("light", currentTheme === "light");
            $("theme-icon").textContent = currentTheme === "dark" ? "🌙" : "☀️";
        }

        function toggleTheme() {
            currentTheme = currentTheme === "dark" ? "light" : "dark";
            localStorage.setItem("vp_theme", currentTheme);
            applyTheme();
        }

        // ==================== Render ====================
        function render() {
            const t = I18N[currentLang];
            const usageContainer = $("usage-value-container");
            const daysContainer = $("days-value-container");
            const ring = $("days-ring");

            // ========== Usage ==========
            if (isUnlimitedGB) {
                usageContainer.innerHTML = `
                    <span class="infinity">∞</span>
                    <span class="infinity-text">${t.unlimited}</span>
                `;
                $("usage-progress").style.width = "100%";
            } else {
                const used = parseFloat(DATA.totalGb) || 0;
                const total = parseFloat(DATA.limitTotalGb) || 0;
                const remaining = Math.max(0, total - used);
                const percent = parseFloat(DATA.totalPercent) || 0;

                usageContainer.innerHTML = `
                    <span class="num">${formatNumber(used)}</span>
                    <span class="unit">/</span>
                    <span class="num">${formatNumber(total)}</span>
                    <span class="unit unit-accent">GB</span>
                `;

                $("used-percent").textContent = formatNumber(percent, 1);
                $("remaining-gb").textContent = formatNumber(remaining);

                setTimeout(() => {
                    $("usage-progress").style.width = Math.min(100, percent) + "%";
                }, 200);
            }

            // ========== Days ==========
            if (isUnlimitedDays) {
                daysContainer.innerHTML = `
                    <span class="infinity">∞</span>
                    <span class="infinity-text">${t.unlimited}</span>
                `;
                $("expiry-date").textContent = t.no_expiry;
                ring.innerHTML = "";
                for (let i = 0; i < 7; i++) {
                    const seg = document.createElement("div");
                    seg.className = "day-seg active";
                    ring.appendChild(seg);
                }
            } else {
                const result = calcDaysLeft(DATA.expiryDate);
                if (result.expired) {
                    daysContainer.innerHTML = `
                        <span class="num">0</span>
                        <span class="unit unit-accent">${t.expired}</span>
                    `;
                } else {
                    daysContainer.innerHTML = `
                        <span class="num">${result.days}</span>
                        <span class="unit unit-accent">${t.days_unit}</span>
                    `;
                }
                $("expiry-date").textContent = toLocaleDate(DATA.expiryDate, currentLang);
                const daysLeft = result.days;
                const segments = 7;
                const filledSegments = daysLeft >= 30
                    ? segments
                    : Math.max(0, Math.round((daysLeft / 30) * segments));
                ring.innerHTML = "";
                for (let i = 0; i < segments; i++) {
                    const seg = document.createElement("div");
                    seg.className = "day-seg" + (i < filledSegments ? " active" : "");
                    ring.appendChild(seg);
                }
            }
        }

        // ==================== Init ====================
        (function init() {
            applyTheme();
            applyLang();
        })();
    </script>

</body>
</html>
